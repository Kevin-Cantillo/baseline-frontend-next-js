/* eslint-disable no-param-reassign */
module.exports = {
  async rewrites() {
    return [
      /* {
        source: '/:path*',
        destination: '/products/:path*',
      }, */
    ];
  },
  webpack5: true,
};
